{
  "name": "format-error-recovers",
  "description": "Two format errors, then a clean command resets the counter, then an explicit submit. Exercises the consecutive-error reset.",
  "task": "recover from format errors and submit",
  "config": {
    "cost_limit": 3.0,
    "step_limit": 0,
    "max_consecutive_format_errors": 3
  },
  "model": {
    "model_name": "replay/deterministic",
    "cost_per_call": 0.01,
    "steps": [
      {"reasoning": "No tool call yet.", "format_error": "Every response needs a bash tool call."},
      {"reasoning": "Still forgot the tool call.", "format_error": "Every response needs a bash tool call."},
      {"reasoning": "Now I run a real command.", "command": "echo recovered"},
      {"reasoning": "Submit the result.", "command": "echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT\necho finished"}
    ]
  },
  "environment": {
    "timeout": 30
  }
}
