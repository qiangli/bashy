# bash-5.3 fixtures known to FAIL — the no-regression ratchet baseline.
#
# scripts/ci-bash53-gate.sh runs the bash-5.3 suite and compares the actual
# non-PASS set against this file:
#
#   * a fixture that fails but is NOT listed here  -> a NEW regression, CI fails.
#   * a fixture listed here that now PASSES         -> progress; CI fails and tells
#     you to DELETE its line, so the baseline only ever shrinks and can never
#     drift stale (the same trap the gate itself was added to close).
#
# The goal is an empty file. Every entry is a known bug with a named cause, all
# introduced between sh 0e0f104f (the 86/86 baseline, 2026-06-27) and d5a1bdc2
# while pushing the yash/POSIX conformance frontier — the bash-5.3 gate was
# silently exiting 0, so the collateral damage accumulated unseen. Delete each
# line as its fix lands.
#
# One fixture per line. Blank lines and #-comments ignored. Keep sorted.
#
# (execscript FIXED 2026-07-10: startup-inherited SIGINT from the
#  backgrounded harness no longer appears as a hard-ignored trap listing;
#  explicit BASHY_HARD_IGNORE propagation still preserves child hard ignores.)
# (comsub FIXED 2026-07-10: POSIX command-substitution parsing now lets aliases
#  expand inside "$(...)" and provide the closing ')', matching bash 5.3.)
# cause: sh 8693093d "interp: tighten posix conformance edge cases"
errors
# (func FIXED 2026-07-10: restored the posix-gated special-builtin function-name
#  error — POSIX interpretation 383 — that sh 80ce66ff wrongly removed.)
# (varenv already PASSes under the expect-filtered bash53 harness as of
#  2026-07-10; stale baseline entry removed.)
