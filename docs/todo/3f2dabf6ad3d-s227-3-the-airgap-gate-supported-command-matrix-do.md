---
id: 3f2dabf6ad3d
kind: test
title: 'S227.3 The airgap gate: supported-command matrix (docs/airgap-image.md) proven by smoke-airgap-container, run in CI on linux amd64/arm64 as a release gate'
seq: 307
status: doing
priority: p1
labels:
    - airgap
    - docs
    - gate
created: 2026-09-20T21:23:20.154291Z
sprint: 227
---

DELIVERED (corbel, 2026-09-21) — CI proof on the candidate tag pending (see evidence).

What landed:
- `scripts/airgap-container-smoke.sh` (`make smoke-airgap-container`, `bashy dag smoke-airgap`): builds the image (`dag build-image`) or probes `AIRGAP_IMAGE`; one probe per row under `--network=none --read-only --cap-drop=ALL`: shell (`--version`, `-c`, `--posix`, `--bashsharp` on examples/quickstart hello/kwargs/enums vs their .expected, Stage 0 `$HOME`, no network tools) + every builtin (61), every coreutil (117 + 61 listed twice as builtins), every yoke verb (122) — the rows come from the image's own `bashy commands --all --json`, never a hand list. Verbs are probed as `/bashy <verb>` (hidden front-door aliases have no bare shim); coreutils bare and in-process (no `timeout` wrapper — it would exec). Engines, remote-by-design verbs and bin-managed externals are classified "not usable offline" by design; anything else missing is a FAIL. SKIP (exit 0) without a usable engine.
- `docs/airgap-image.md`: prose (how it is built, what bashy fetches per OS, "What the host needs" per OS) + the table between markers, REGENERATED by the script (`AIRGAP_WRITE_DOC=1`); without the flag the script diffs the doc against the measurement and fails on any difference — one source. Notes normalize arch/version/sha tokens so amd64 and arm64, dev and release builds measure the same table.
- `.github/workflows/airgap-image.yml`: linux amd64 (ubuntu-latest) + arm64 (ubuntu-24.04-arm, native) on `v*` tags, `workflow_dispatch`, and PRs touching the gate; PATH scrubbed of podman/docker so `bashy podman` provisions itself (cold cache), then the smoke; sizes to the job summary.

Measured on the Linux test host (image built from HEAD's scratch artifact, managed podman): 360 rows — 303 works, 6 present (man/time/xargs/full/run/transpile answer, but not --help), 50 not usable offline by design, 0 FAIL; second run in diff mode: "doc table matches the measured rows" PASS. Wall 15 s.

Acceptance: SKIP without engine ✔ (by construction); FAIL on mismatch ✔ (measured before the probe fixes: 4 false rows failed the gate); doc regenerated from the script ✔; umbrella INDEX/README line — with the pin bump; workflow green on the candidate — pending the `-dev` tag.
