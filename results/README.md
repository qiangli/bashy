# Runtime Results

Raw runtime and evaluation output is intentionally local-only.

These files often include absolute paths, usernames, host/container metadata,
command transcripts, provider trace IDs, token usage, and occasionally secrets
captured from failing commands. Do not commit raw result files.

If a benchmark result should be published, create a sanitized summary under
`docs/` or another reviewed location instead of committing the raw run output.
